[build]
builder = "nixpacks"

[deploy]
startCommand = "python implementation/api_server.py"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
