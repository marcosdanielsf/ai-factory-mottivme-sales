---
phase: 09-foundation
plan: "02"
type: execute
wave: 2
depends_on:
  - "09-01"
files_modified:
  - .planning/phases/09-foundation/workflows/09-cjm-event-ingester.json
  - .planning/phases/09-foundation/workflows/09-cjm-backfill.json
autonomous: false
requirements:
  - DATA-01
  - DATA-03

must_haves:
  truths:
    - "Enviar o mesmo payload GHL duas vezes insere exatamente 1 row em cjm_events (ON CONFLICT DO NOTHING)"
    - "Uma stage transition em qualquer dos 4 pipelines GHL aparece em cjm_events em até 30 segundos"
    - "Dados históricos dos 4 pipelines GHL para os 4 clientes ativos existem em cjm_events após backfill"
    - "Backfill não duplica eventos ao ser rodado duas vezes (idempotência via UNIQUE constraint)"
  artifacts:
    - path: ".planning/phases/09-foundation/workflows/09-cjm-event-ingester.json"
      provides: "Export JSON do workflow n8n de ingestão live"
    - path: ".planning/phases/09-foundation/workflows/09-cjm-backfill.json"
      provides: "Export JSON do workflow n8n de backfill manual"
  key_links:
    - from: "n8n Code node (stage_key builder)"
      to: "cjm_events.UNIQUE(contact_id, stage_key, source_event_id)"
      via: "stage_key = pipeline_id + '.' + stage_id"
      pattern: "stageKey.*pipeline_id.*stage_id"
    - from: "n8n Postgres INSERT node"
      to: "cjm_events"
      via: "ON CONFLICT (contact_id, stage_key, source_event_id) DO NOTHING"
      pattern: "ON CONFLICT.*DO NOTHING"
    - from: "n8n Postgres UPSERT node"
      to: "cjm_journey_state"
      via: "ON CONFLICT (location_id, contact_id, pipeline_id) DO UPDATE SET"
      pattern: "ON CONFLICT.*location_id.*contact_id.*pipeline_id"

user_setup:
  - service: n8n
    why: "Configurar GHL webhook trigger apontando para a URL do workflow CJM Event Ingester"
    dashboard_config:
      - task: "No GHL > Settings > Integrations > Webhooks: adicionar URL do n8n ingester para o evento OpportunityStageUpdate (ou usar Workflow Trigger no GHL automations)"
        location: "GHL Dashboard > Settings > Integrations"
      - task: "No n8n: verificar que as credenciais GHL PIT (Private Integration Token) de cada cliente estão configuradas como credentials separadas — não hardcoded nos Code nodes"
        location: "n8n > Credentials"
---

<objective>
Criar 2 workflows n8n: (1) CJM Event Ingester — recebe webhooks GHL de stage transitions e insere em cjm_events + cjm_journey_state com idempotência; (2) CJM Backfill — percorre os 4 pipelines × 4 clientes ativos via GHL REST API v2 e semeia dados históricos com rate limiting.

Purpose: Sem os workflows, o schema fica vazio e nenhuma fase visual pode funcionar.
Output: 2 arquivos JSON de workflow (exportados do n8n) salvos em .planning/phases/09-foundation/workflows/ para versionamento.
</objective>

<execution_context>
@/Users/marcosdaniels/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marcosdaniels/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-foundation/09-RESEARCH.md
@.planning/phases/09-foundation/09-01-SUMMARY.md
</context>

<interfaces>
<!-- Contratos do schema criado em 09-01. Executor usa diretamente — sem explorar o codebase. -->

Tabela cjm_events (INSERT target):

- location_id TEXT NOT NULL
- contact_id TEXT NOT NULL
- pipeline_id TEXT NOT NULL
- stage_key TEXT NOT NULL -- formato: 'pipeline_id.stage_id'
- from_stage TEXT -- NULLIF(..., 'NULL') obrigatório
- to_stage TEXT NOT NULL
- event_type TEXT NOT NULL -- usar 'stage_change' para transições GHL
- source_event_id TEXT -- GHL delivery/dedup ID
- metadata JSONB
- occurred_at TIMESTAMPTZ NOT NULL -- da payload, NÃO now()
  UNIQUE(contact_id, stage_key, source_event_id) -- ON CONFLICT DO NOTHING

Tabela cjm_journey_state (UPSERT target):

- location_id TEXT NOT NULL
- contact_id TEXT NOT NULL
- pipeline_id TEXT NOT NULL
- current_stage TEXT NOT NULL
- entered_stage_at TIMESTAMPTZ NOT NULL
- sla_status TEXT DEFAULT 'ok' -- sempre 'ok' ao entrar numa etapa
- last_event_at TIMESTAMPTZ
  UNIQUE(location_id, contact_id, pipeline_id) -- ON CONFLICT DO UPDATE SET

GHL Pipeline IDs (verificar contra conta live antes do backfill):

- Prospects: zay1uZBOKpheJKFlk2Il
- Pre-Vendas: 5LwcbrLUXG6TCLaP9wf3
- Sales Farming: cKc7qtxHdyVqG7aPkl3H
- CS/Retencao: QMG7pyGM6hlbvHrDPBVD

Clientes ativos para backfill (4): MOTTIVME, Dra. Gabriela, Instituto Amare, Dra. Carolina
Clientes a pular no backfill: Alberto (churned), Eline (churned)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Workflow CJM Event Ingester (live GHL webhook ingestion)</name>
  <files>.planning/phases/09-foundation/workflows/09-cjm-event-ingester.json</files>
  <action>
Criar o workflow n8n "CJM Event Ingester" diretamente via n8n UI (não via API — evitar o bug de recompilação de Code nodes que introduz `\!`). Seguir EXATAMENTE a arquitetura do RESEARCH.md Pattern 5.

**Sequência de nodes (max 10 nodes):**

```
1. Webhook Trigger
   - Method: POST
   - Path: /cjm-event-ingester
   - Responde imediatamente com HTTP 200 (não aguardar processamento)

2. Set: Extrair campos do payload GHL
   - Mapear: location_id, contact_id, pipeline_id, stage_id (to_stage), old_stage_id (from_stage), dateAdded (occurred_at)
   - GHL pode usar camelCase ou snake_case — mapear ambos com fallback:
     location_id = {{ $json.body.location_id || $json.body.locationId }}
     contact_id  = {{ $json.body.contact_id || $json.body.contactId }}
     pipeline_id = {{ $json.body.pipeline_id || $json.body.pipelineId }}
     to_stage    = {{ $json.body.stage_id || $json.body.newStageId || $json.body.stageId }}
     from_stage  = {{ $json.body.old_stage_id || $json.body.previousStageId || '' }}
     occurred_at = {{ $json.body.dateAdded || $json.body.timestamp || new Date().toISOString() }}

3. Code: Build stage_key + source_event_id
   REGRA CRITICA: NÃO usar ! em strings ou comentários (bug reincidência 4x)

   const body = $input.first().json;
   const headers = $input.first().headers || {};

   // GHL dedup ID — verificar header exato no primeiro teste live
   const sourceEventId =
     headers['x-webhook-delivery-id'] ||
     headers['x-ghl-signature'] ||
     [body.contact_id, body.pipeline_id, body.to_stage, body.occurred_at].join('|');

   const stageKey = body.pipeline_id + '.' + body.to_stage;

   return [{
     json: {
       ...body,
       stage_key: stageKey,
       source_event_id: sourceEventId,
       metadata: JSON.stringify({
         raw_event_type: $input.first().json.type,
         opportunity_id: body.opportunity_id || body.id || null,
       }),
     },
   }];

4. Postgres: INSERT cjm_events (ON CONFLICT DO NOTHING)
   Operation: Execute Query
   Query (template literals — NÃO $1/$2 parametrizados):

   INSERT INTO cjm_events (
     location_id, contact_id, pipeline_id, stage_key,
     from_stage, to_stage, event_type, source_event_id,
     metadata, occurred_at
   ) VALUES (
     '{{ $json.location_id }}',
     '{{ $json.contact_id }}',
     '{{ $json.pipeline_id }}',
     '{{ $json.stage_key }}',
     NULLIF('{{ $json.from_stage }}', 'NULL'),
     '{{ $json.to_stage }}',
     'stage_change',
     '{{ $json.source_event_id }}',
     '{{ $json.metadata }}'::jsonb,
     '{{ $json.occurred_at }}'::timestamptz
   )
   ON CONFLICT (contact_id, stage_key, source_event_id) DO NOTHING;

   alwaysOutputData: true (sem isso, 0 rows na duplicata mata a chain)

5. Postgres: UPSERT cjm_journey_state
   Operation: Execute Query

   INSERT INTO cjm_journey_state (
     location_id, contact_id, pipeline_id, current_stage,
     entered_stage_at, sla_status, last_event_at
   ) VALUES (
     '{{ $json.location_id }}',
     '{{ $json.contact_id }}',
     '{{ $json.pipeline_id }}',
     '{{ $json.to_stage }}',
     '{{ $json.occurred_at }}'::timestamptz,
     'ok',
     '{{ $json.occurred_at }}'::timestamptz
   )
   ON CONFLICT (location_id, contact_id, pipeline_id)
   DO UPDATE SET
     current_stage    = EXCLUDED.current_stage,
     entered_stage_at = EXCLUDED.entered_stage_at,
     sla_status       = 'ok',
     last_event_at    = EXCLUDED.last_event_at,
     updated_at       = now();

   alwaysOutputData: true

6. Respond to Webhook: HTTP 200
   Body: { "status": "ok" }
```

**Credenciais:**

- Postgres: usar a credential Supabase existente (projeto bfumywvwubvernvhjehk) — service_role key bypassa RLS
- NÃO hardcodar tokens

**Após criar no n8n UI:**

1. Verificar: abrir cada Code node e buscar por `\!` — se encontrar, corrigir para `!` no UI
2. Testar com payload simulado antes de ativar
3. Exportar o workflow: Menu > Download/Export > salvar como `09-cjm-event-ingester.json`
4. Salvar o JSON em `.planning/phases/09-foundation/workflows/09-cjm-event-ingester.json`

**Teste de idempotência (OBRIGATÓRIO antes de marcar done):**
Enviar o mesmo payload POST duas vezes para o webhook URL.
Verificar: `SELECT COUNT(*) FROM cjm_events WHERE contact_id = '[contact_id_do_teste]'` = 1 (não 2).
</action>
<verify>
<automated>
-- No Supabase SQL editor após teste de idempotência:
-- 1. Contar rows: deve ser 1 após 2 envios idênticos
SELECT COUNT(\*) FROM cjm_events WHERE source_event_id = '[source_event_id_do_teste]';
-- Esperado: 1

-- 2. Verificar que cjm_journey_state foi atualizado:
SELECT contact_id, current_stage, sla_status FROM cjm_journey_state WHERE contact_id = '[contact_id_do_teste]';
-- Esperado: 1 row com sla_status = 'ok'
</automated>
</verify>
<done>

- Workflow "CJM Event Ingester" ativo no n8n com webhook URL configurado
- Teste de idempotência passou: 2 envios idênticos = 1 row em cjm_events
- cjm_journey_state atualizado corretamente após evento
- Export JSON salvo em .planning/phases/09-foundation/workflows/09-cjm-event-ingester.json
- Nenhum `\!` encontrado nos Code nodes após verificação
  </done>
  </task>

<task type="auto">
  <name>Task 2: Workflow CJM Backfill (bulk histórico 4 pipelines x 4 clientes)</name>
  <files>.planning/phases/09-foundation/workflows/09-cjm-backfill.json</files>
  <action>
Criar o workflow n8n "CJM Backfill" para semear dados históricos via GHL REST API v2. Trigger manual — rodar uma vez por cliente, nunca em paralelo com o Event Ingester.

**Estratégia de backfill (vide RESEARCH.md Open Questions — recomendação aceita):**
Inserir apenas o estado atual de cada oportunidade como um único `stage_change` event por contact/pipeline. NÃO tentar reconstruir histórico completo (requer GHL activity log API com rate limits diferentes). A success criteria do Phase 9 é "queryable by client_id" — estado atual satisfaz isso.

**Sequência de nodes (max 15 nodes):**

```
1. Manual Trigger

2. Set: Config
   - pipeline_ids: ['zay1uZBOKpheJKFlk2Il', '5LwcbrLUXG6TCLaP9wf3', 'cKc7qtxHdyVqG7aPkl3H', 'QMG7pyGM6hlbvHrDPBVD']
   - location_id: [CONFIGURAR MANUALMENTE ANTES DE RODAR — cada execução é para 1 cliente]
   - IMPORTANTE: verificar pipeline IDs contra conta live GHL antes da primeira execução

3. SplitInBatches (outer): tamanho 1 — iterar um pipeline por vez
   - Input: os 4 pipeline IDs do Set acima
   - NUNCA usar SplitInBatches aninhado direto — encadear sequencialmente

4. HTTP Request: GET /opportunities (GHL API v2)
   URL: https://services.leadconnectorhq.com/opportunities/search
   Method: GET
   Query params:
     pipelineId: {{ $json.pipeline_id }}
     locationId: {{ $('Set').item.json.location_id }}
     limit: 100
   Headers:
     Authorization: Bearer [GHL PIT token — usar n8n credential, NÃO hardcoded]
     Version: 2021-07-28

   alwaysOutputData: true (evitar chain break em pipeline vazio)

5. Wait: 2000ms (rate limit guard entre pipelines)

6. Code: Transform opportunities para cjm_events rows
   REGRA CRITICA: NÃO usar ! em strings

   const response = $input.first().json;
   const opportunities = response.opportunities || [];
   const locationId = $('Set').first().json.location_id;
   const pipelineId = $input.first().json.pipelineId || '';

   return opportunities.map(opp => {
     const stageKey = pipelineId + '.' + (opp.pipelineStageId || opp.stage_id || '');
     return {
       json: {
         location_id: locationId,
         contact_id:  opp.contactId || opp.contact_id,
         pipeline_id: pipelineId,
         stage_key:   stageKey,
         from_stage:  null,
         to_stage:    opp.pipelineStageId || opp.stage_id || '',
         source_event_id: 'backfill_' + (opp.id || ''),
         occurred_at: opp.createdAt || opp.dateAdded || new Date().toISOString(),
         metadata: JSON.stringify({
           backfill: true,
           opportunity_name: opp.name,
           opportunity_id:   opp.id,
           opportunity_status: opp.status,
         }),
       },
     };
   });

7. SplitInBatches (inner): tamanho 20 — processar em lotes de 20
   ATENÇÃO: Não aninhar SplitInBatches. Estruturar como: outer loop fecha antes de inner começar.
   Alternativa segura: usar SplitInBatches único de tamanho 20, sem outer loop aninhado.
   Se necessário, rodar o workflow manualmente uma vez por pipeline_id.

8. Wait: 500ms (entre batches)

9. Postgres: INSERT cjm_events (ON CONFLICT DO NOTHING)
   Mesma query da Task 1 — reutilizar padrão idêntico.
   alwaysOutputData: true

10. Postgres: UPSERT cjm_journey_state
    Mesma query da Task 1 — reutilizar padrão idêntico.
    alwaysOutputData: true
```

**AVISO CRÍTICO sobre SplitInBatches aninhado:**
NUNCA usar dois SplitInBatches um dentro do outro (reincidência documentada — causa loop infinito ou pula itens). Se a lógica precisar de outer loop (clientes) + inner loop (batches), usar execuções separadas por cliente — disparar manualmente para cada location_id.

**Procedimento de execução do backfill:**

1. No Set node, configurar o `location_id` do cliente a ser backfillado
2. Confirmar que o Event Ingester NÃO está rodando para esse location_id simultaneamente
3. Executar manualmente
4. Verificar: `SELECT COUNT(*) FROM cjm_events WHERE location_id = '[location_id]'`
5. Repetir para cada um dos 4 clientes ativos (MOTTIVME, Dra. Gabriela, Instituto Amare, Dra. Carolina)
6. Clientes churned (Alberto, Eline): consultar com Marcos se quer dados históricos antes de rodar

**Após criar no n8n UI:**

1. Verificar: abrir Code node e buscar por `\!` — corrigir se encontrar
2. Desativar o workflow após backfill completo (não deve ficar ativo)
3. Exportar: Menu > Download/Export > salvar como `09-cjm-backfill.json`
4. Salvar em `.planning/phases/09-foundation/workflows/09-cjm-backfill.json`
   </action>
   <verify>
   <automated>
   -- Após rodar backfill para 1 cliente de teste:
   -- 1. Contar eventos inseridos por pipeline:
   SELECT pipeline_id, COUNT(\*) as event_count
   FROM cjm_events
   WHERE location_id = '[location_id_do_cliente_teste]'
   GROUP BY pipeline_id;
   -- Esperado: pelo menos 1 pipeline com > 0 events

-- 2. Verificar que backfill é idempotente (rodar segunda vez não duplica):
-- Contar antes, rodar backfill novamente, contar depois — deve ser o mesmo número
SELECT COUNT(\*) FROM cjm_events WHERE location_id = '[location_id_do_cliente_teste]';
</automated>
</verify>
<done>

- Workflow "CJM Backfill" criado no n8n (pode estar inativo — trigger manual)
- Backfill executado com sucesso para pelo menos 1 cliente (validação de funcionamento)
- Segundo run do backfill para o mesmo cliente não cria duplicatas (ON CONFLICT DO NOTHING)
- Export JSON salvo em .planning/phases/09-foundation/workflows/09-cjm-backfill.json
- Nenhum `\!` encontrado nos Code nodes após verificação
  </done>
  </task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verificar idempotência E2E e workflows n8n funcionando</name>
  <action>Verificar manualmente que os 2 workflows n8n foram criados corretamente e que o teste de idempotência passou antes de prosseguir para Phase 10.</action>
  <verify><automated>ls .planning/phases/09-foundation/workflows/ 2>/dev/null</automated></verify>
  <done>Idempotência confirmada: 2 payloads idênticos = 1 row em cjm_events. Ambos os workflows exportados em .planning/phases/09-foundation/workflows/</done>
  <what-built>
    Dois workflows n8n completos:
    1. CJM Event Ingester — webhook live recebendo stage transitions GHL
    2. CJM Backfill — bulk histórico manual para 4 pipelines x clientes ativos

    Idempotência verificada via teste de payload duplicado.
    Exports JSON salvos em .planning/phases/09-foundation/workflows/

  </what-built>
  <how-to-verify>
    1. No n8n, abrir "CJM Event Ingester" — verificar que está ATIVO e tem 6 nodes
    2. Copiar o webhook URL do node de trigger
    3. Enviar POST de teste com payload simulado:
       curl -X POST [WEBHOOK_URL] \
         -H "Content-Type: application/json" \
         -d '{"locationId":"test-loc","contactId":"test-contact-001","pipelineId":"zay1uZBOKpheJKFlk2Il","stageId":"test-stage","dateAdded":"2026-03-11T12:00:00Z"}'
    4. Verificar no Supabase: SELECT COUNT(*) FROM cjm_events WHERE contact_id = 'test-contact-001' — esperado: 1
    5. Enviar o mesmo payload novamente
    6. Verificar: COUNT ainda é 1 (idempotência confirmada)
    7. Verificar: SELECT * FROM cjm_journey_state WHERE contact_id = 'test-contact-001' — 1 row com sla_status='ok'
    8. No n8n, abrir "CJM Backfill" — verificar que existe (pode estar inativo)
    9. Verificar: ls .planning/phases/09-foundation/workflows/ mostra 2 arquivos JSON

    PENDENTE DO MARCOS: validar 4 GHL pipeline IDs contra conta live antes de rodar backfill nos clientes reais (STATE.md todo item)

  </how-to-verify>
  <resume-signal>
    Digite "aprovado" se idempotência confirmada e workflows criados.
    Descreva qualquer problema encontrado (ex: "header GHL diferente", "0 rows no backfill") para diagnóstico.
  </resume-signal>
</task>

</tasks>

<verification>
Phase 9 completa quando TODOS os success criteria do ROADMAP forem verdadeiros:
1. Payload idêntico enviado 2x → exatamente 1 row em cjm_events (verificado na Task 1)
2. Stage transition GHL → cjm_events em ≤30s (verificado ativando o Event Ingester e fazendo move manual no GHL)
3. Dados históricos de todos os clientes ativos existem em cjm_events após backfill (verificado na Task 2)
4. SELECT business_hours_diff('2026-03-07 18:00:00+03', '2026-03-09 09:00:00+03') retorna 0 (verificado em 09-01)

Checklist de gotchas críticos:

- [ ] Nenhum `\!` nos Code nodes (verificar após qualquer PUT via API)
- [ ] NULLIF usado para from_stage (n8n serializa null como string "NULL")
- [ ] occurred_at vem da payload, não de now()
- [ ] Backfill e Event Ingester não rodando simultaneamente para o mesmo location_id
      </verification>

<success_criteria>

- Idempotência E2E: 2 webhooks idênticos = 1 row (verified via curl + SELECT)
- Latência live: stage transition GHL → Supabase em < 30s em execução real
- Backfill: pelo menos 1 cliente com dados em cjm_events após backfill manual
- Export JSONs: ambos os workflows versionados em .planning/phases/09-foundation/workflows/
- Phase 10 desbloqueada: os 4 pipelines IDs funcionando confirmados, dados disponíveis para `vw_cjm_pipeline_flow`
  </success_criteria>

<output>
Após completion, criar `.planning/phases/09-foundation/09-02-SUMMARY.md` com:
- Workflow IDs no n8n (para referência futura)
- Webhook URL do Event Ingester (para configuração GHL)
- Resultado do teste de idempotência (antes/depois contagem)
- Resultado do backfill por cliente (quantos eventos inseridos por pipeline)
- Header GHL real para source_event_id (descoberto no primeiro teste live)
- Qualquer desvio do plano e motivo
</output>
